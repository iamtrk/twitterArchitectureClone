#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../streamline/bin/node-streamline" "$@"
  ret=$?
else 
  node  "$basedir/../streamline/bin/node-streamline" "$@"
  ret=$?
fi
exit $ret
