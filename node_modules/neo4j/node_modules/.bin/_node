#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../streamline/bin/_node" "$@"
  ret=$?
else 
  node  "$basedir/../streamline/bin/_node" "$@"
  ret=$?
fi
exit $ret
